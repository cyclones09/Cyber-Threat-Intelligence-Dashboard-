# OBSIDIAN — dark web forum recon engine
# Core web framework + server
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
# Async HTTP client with SOCKS (Tor) support
httpx[socks]>=0.27,<1.0
# HTML parsing for forum scraping
beautifulsoup4>=4.12,<5.0
lxml>=5.0
# Config + validation
pydantic>=2.5,<3.0
pydantic-settings>=2.1,<3.0
PyYAML>=6.0
# LLM layer (optional — query refinement, result filtering, summaries).
# The pipeline runs without it; install + set ANTHROPIC_API_KEY to enable.
anthropic>=0.40,<1.0
